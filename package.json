{
  "name": "conventional-commit-validator",
  "version": "1.0.0",
  "description": "GitHub Action to validate commit messages follow Conventional Commits format",
  "main": "dist/index.js",
  "scripts": {
    "build": "ncc build commit-message-validator.js -o dist",
    "test": "jest"
  },
  "keywords": [
    "github",
    "actions",
    "conventional-commits",
    "validation"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.0",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
